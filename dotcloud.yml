www:
  type: ruby
mobwrite:
  type: custom
  buildscript: mobwrite/builder
  approot: mobwrite
  process: ~/daemon/mobwrite_daemon.py
  ports:
    mobwrite: tcp
redis:
  type: redis
db:
  type: postgresql